# services/ml

Layanan Machine Learning EcoSentra (FastAPI/Python).

Status: **placeholder** — belum ada kode. Folder ini disiapkan sebagai lokasi
untuk layanan ML (mis. prediksi tutupan lahan, analisis NDVI, deteksi
deforestasi).

Kerjakan lewat branch `feature/ml-*` dari `develop`, contoh:

```bash
git checkout develop
git pull origin develop
git checkout -b feature/ml-predict
```

Lihat [`docs/git-workflow.md`](../../docs/git-workflow.md) di root repo untuk
konvensi branch dan alur kerja lengkap.
