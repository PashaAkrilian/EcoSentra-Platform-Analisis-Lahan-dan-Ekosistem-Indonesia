# services/mcp

MCP (Model Context Protocol) server EcoSentra (Python).

Status: **placeholder** — belum ada kode. Folder ini disiapkan sebagai lokasi
untuk MCP server yang akan mengekspos tools/data EcoSentra ke asisten AI.

Kerjakan lewat branch `feature/mcp-*` dari `develop`, contoh:

```bash
git checkout develop
git pull origin develop
git checkout -b feature/mcp-query-tool
```

Lihat [`docs/git-workflow.md`](../../docs/git-workflow.md) di root repo untuk
konvensi branch dan alur kerja lengkap.
